@*@model List<TechnicoWebApi.Dtos.OwnerResponseDto>

@{
    ViewData["Title"] = "Choose Owner for the new Property";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Create</h1>

<h4>PropertyItem</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateProperty">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
             <div class="form-group">
                <label asp-for="Own" class="control-label"></label>
                <select asp-for="PropertyType" class="form-control">
                        @foreach (var type in Enum.GetValues(typeof(TechnicoWebApi.Models.PropertyType)))
                        {
                            <option value="@type">@type</option>
                        }
                </select>
                <span asp-validation-for="PropertyType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="CreateProperty">Back</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}*@
